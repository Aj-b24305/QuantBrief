[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "risksentry"
version = "0.1.0"
description = "RiskSentry - deterministic portfolio risk engine (FastAPI + NumPy/Pandas) with LLM memo synthesis"
readme = "README.md"
requires-python = ">=3.11"
license = { text = "MIT" }
authors = [{ name = "RiskSentry" }]
keywords = ["risk", "portfolio", "var", "beta", "sharpe", "fastapi", "quant", "llm"]
classifiers = [
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Topic :: Office/Business :: Financial",
    "Topic :: Scientific/Engineering :: Mathematics",
]
dependencies = [
    "fastapi>=0.115.0",
    "uvicorn[standard]>=0.30.0",
    "pydantic>=2.7.0",
    "pydantic-settings>=2.3.0",
    "numpy>=1.26.0",
    "pandas>=2.2.0",
    "yfinance>=0.2.40",
    "openai>=1.40.0",
    "httpx>=0.27.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0.0",
    "pytest-cov>=5.0.0",
    "mypy>=1.10.0",
    "ruff>=0.5.0",
]

[project.scripts]
risksentry = "risksentry.main:main"

[tool.hatch.build.targets.wheel]
packages = ["risksentry"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q --disable-warnings"

[tool.mypy]
python_version = "3.12"  # 3.12+ so numpy's bundled stubs (which use `type` statements) parse
ignore_missing_imports = true
check_untyped_defs = true
disallow_untyped_defs = true
warn_redundant_casts = true
warn_unused_ignores = true

[tool.ruff]
line-length = 110
target-version = "py311"